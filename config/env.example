# ==================== 应用配置 ====================
APP_NAME=RaspberryCloud
APP_VERSION=1.0.0
DEBUG=false

# ==================== 安全配置 ====================
# 生成密钥: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ==================== 数据库配置 ====================
# SQLite（推荐≤3用户）
DATABASE_URL=sqlite:////opt/raspberrycloud/backend/raspberrycloud.db

# MariaDB（推荐3+用户，取消注释并配置）
# DATABASE_URL=mysql+pymysql://clouduser:your_password@localhost/raspberrycloud

# ==================== 存储路径配置 ====================
# SD卡存储方案（64GB SD卡推荐配置）
# 系统占用约10GB，剩余约50GB用于数据存储
STORAGE_PATH=/opt/raspberrycloud/storage/users
SHARE_PATH=/opt/raspberrycloud/storage/shares
TEMP_PATH=/opt/raspberrycloud/storage/temp
BACKUP_PATH=/opt/raspberrycloud/storage/backups

# 如果使用外接硬盘，取消注释并修改为：
# STORAGE_PATH=/mnt/cloud_storage/users
# SHARE_PATH=/mnt/cloud_storage/shares
# TEMP_PATH=/mnt/cloud_storage/temp
# BACKUP_PATH=/mnt/cloud_storage/backups

# ==================== 文件限制 ====================
MAX_FILE_SIZE=10737418240
MAX_UPLOAD_THREADS=5
CHUNK_SIZE=5242880

# ==================== 默认管理员配置 ====================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=RaspberryCloud2024!
ADMIN_EMAIL=admin@raspberrycloud.local

# ==================== 用户配额（字节）====================
# SD卡存储方案：64GB SD卡，系统10GB，数据50GB，建议单用户配额20-30GB
DEFAULT_USER_QUOTA=21474836480  # 20GB（SD卡方案）
# 外接硬盘方案：可设置为更大值，如100GB
# DEFAULT_USER_QUOTA=107374182400  # 100GB

# ==================== 分享配置 ====================
SHARE_LINK_LENGTH=8
MAX_SHARE_DAYS=7

# ==================== 邮件配置（邮箱验证）====================
# SMTP服务器配置（用于发送验证码）
# 常用邮箱SMTP配置：
# QQ邮箱: smtp.qq.com:587
# 163邮箱: smtp.163.com:25
# Gmail: smtp.gmail.com:587
# 企业邮箱: 咨询邮箱服务商

SMTP_HOST=smtp.qq.com
SMTP_PORT=587
SMTP_USER=your-email@qq.com
SMTP_PASSWORD=your-email-password-or-auth-code
SMTP_FROM=your-email@qq.com

# 注意：
# - QQ邮箱需要使用"授权码"而不是登录密码
# - 获取授权码：QQ邮箱 → 设置 → 账户 → 开启SMTP服务 → 生成授权码
# - 如果未配置SMTP，开发环境下验证码会打印在控制台


