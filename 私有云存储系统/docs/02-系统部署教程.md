# 系统部署教程

完整的后端、前端、数据库、Web服务器部署指南。

## 📋 前置条件

确保已完成[硬件准备与初始化](01-硬件准备与初始化.md)：
- ✅ 树莓派系统已烧录并更新
- ✅ 外接存储已挂载到 `/mnt/cloud_storage`
- ✅ 静态IP已设置
- ✅ SSH已开启并可连接

## 📦 步骤1：安装系统依赖

### 1.1 安装Python 3.11+

```bash
# 检查Python版本
python3 --version

# 如果版本<3.11，安装最新版
sudo apt update
sudo apt install -y python3 python3-pip python3-venv

# 安装开发工具
sudo apt install -y python3-dev build-essential libssl-dev libffi-dev
```

### 1.2 安装数据库

#### 选项A：SQLite（推荐，适合≤3用户）

```bash
# SQLite已预装在Raspberry Pi OS
sqlite3 --version

# 如果没有，安装：
sudo apt install -y sqlite3 libsqlite3-dev
```

#### 选项B：MariaDB（适合3+用户）

```bash
# 安装MariaDB
sudo apt install -y mariadb-server mariadb-client

# 启动服务
sudo systemctl start mariadb
sudo systemctl enable mariadb

# 安全配置
sudo mysql_secure_installation
# 按提示操作：
# - 设置root密码（记住此密码）
# - 删除匿名用户：Y
# - 禁止root远程登录：Y
# - 删除测试数据库：Y
# - 重新加载权限：Y

# 创建应用数据库和用户
sudo mysql -u root -p
```

在MySQL提示符下执行：

```sql
CREATE DATABASE raspberrycloud CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'clouduser'@'localhost' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON raspberrycloud.* TO 'clouduser'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 1.3 安装Nginx

```bash
sudo apt install -y nginx

# 启动服务
sudo systemctl start nginx
sudo systemctl enable nginx

# 测试（浏览器访问 http://树莓派IP，应显示Nginx欢迎页）
```

### 1.4 安装Samba（局域网共享）

```bash
sudo apt install -y samba samba-common-bin

# 启动服务
sudo systemctl start smbd
sudo systemctl enable smbd
```

### 1.5 安装其他工具

```bash
# 安装ffmpeg（视频预览缩略图）
sudo apt install -y ffmpeg

# 安装图像处理库
sudo apt install -y libjpeg-dev zlib1g-dev

# 安装supervisor（进程管理，可选）
sudo apt install -y supervisor
```

## 🗂️ 步骤2：下载项目代码

### 2.1 创建项目目录

```bash
# 创建应用目录
sudo mkdir -p /opt/raspberrycloud
sudo chown -R $USER:$USER /opt/raspberrycloud
cd /opt/raspberrycloud

# 如果使用Git（推荐）
git clone <你的仓库地址> .

# 或手动上传文件
# 使用SCP从本地上传：
# scp -r 私有云存储系统/* pi@树莓派IP:/opt/raspberrycloud/
```

### 2.2 验证文件结构

```bash
tree -L 2 /opt/raspberrycloud

# 应该看到：
# /opt/raspberrycloud/
# ├── backend/
# ├── frontend/
# ├── config/
# ├── scripts/
# └── docs/
```

## 🐍 步骤3：部署后端服务

### 3.1 创建Python虚拟环境

```bash
cd /opt/raspberrycloud

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip
```

### 3.2 安装Python依赖

```bash
cd backend

# 安装依赖（需要5-10分钟）
pip install -r requirements.txt

# 如果安装失败，尝试单独安装：
pip install fastapi==0.104.1
pip install uvicorn[standard]==0.24.0
pip install python-multipart==0.0.6
pip install aiofiles==23.2.1
pip install python-jose[cryptography]==3.3.0
pip install passlib[bcrypt]==1.7.4
pip install sqlalchemy==2.0.23
pip install alembic==1.12.1
pip install pillow==10.1.0
pip install python-magic==0.4.27
```

### 3.3 配置环境变量

```bash
# 复制配置模板
cp /opt/raspberrycloud/config/.env.example /opt/raspberrycloud/backend/.env

# 编辑配置
nano /opt/raspberrycloud/backend/.env
```

配置内容：

```bash
# 应用配置
APP_NAME=RaspberryCloud
APP_VERSION=1.0.0
DEBUG=false

# 安全配置
SECRET_KEY=你的随机密钥（使用下面命令生成）
# 生成密钥：openssl rand -hex 32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# 数据库配置（SQLite）
DATABASE_URL=sqlite:////opt/raspberrycloud/backend/raspberrycloud.db

# 或使用MariaDB
# DATABASE_URL=mysql+pymysql://clouduser:你的密码@localhost/raspberrycloud

# 存储配置
STORAGE_PATH=/mnt/cloud_storage/users
SHARE_PATH=/mnt/cloud_storage/shares
TEMP_PATH=/mnt/cloud_storage/temp
BACKUP_PATH=/mnt/cloud_storage/backups

# 文件限制
MAX_FILE_SIZE=10737418240  # 10GB in bytes
MAX_UPLOAD_THREADS=5

# 默认管理员
ADMIN_USERNAME=admin
ADMIN_PASSWORD=RaspberryCloud2024!
ADMIN_EMAIL=admin@raspberrycloud.local
```

生成密钥：

```bash
openssl rand -hex 32
# 将输出复制到.env的SECRET_KEY
```

### 3.4 初始化数据库

```bash
cd /opt/raspberrycloud/backend

# 确保虚拟环境已激活
source /opt/raspberrycloud/venv/bin/activate

# 运行数据库初始化脚本
python -c "from models import init_db; init_db()"

# 或手动执行SQL（如果使用SQLite）
sqlite3 raspberrycloud.db < database.sql
```

### 3.5 测试后端服务

```bash
cd /opt/raspberrycloud/backend
source /opt/raspberrycloud/venv/bin/activate

# 启动测试服务器
uvicorn main:app --host 0.0.0.0 --port 8000

# 打开另一个SSH会话测试
curl http://localhost:8000/api/health

# 预期输出：{"status":"healthy","version":"1.0.0"}

# 测试完成后按 Ctrl+C 停止
```

## 🌐 步骤4：部署前端

### 4.1 复制前端文件

```bash
# 创建Web根目录
sudo mkdir -p /var/www/raspberrycloud

# 复制前端文件
sudo cp -r /opt/raspberrycloud/frontend/* /var/www/raspberrycloud/

# 设置权限
sudo chown -R www-data:www-data /var/www/raspberrycloud
sudo chmod -R 755 /var/www/raspberrycloud
```

### 4.2 配置前端API地址

```bash
sudo nano /var/www/raspberrycloud/js/config.js
```

内容：

```javascript
const API_BASE_URL = window.location.protocol + '//' + window.location.host + '/api';
const WS_BASE_URL = (window.location.protocol === 'https:' ? 'wss:' : 'ws:') + '//' + window.location.host + '/ws';
const MAX_FILE_SIZE = 10 * 1024 * 1024 * 1024; // 10GB
const CHUNK_SIZE = 5 * 1024 * 1024; // 5MB 分块上传
```

## 🔧 步骤5：配置Nginx

### 5.1 备份默认配置

```bash
sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.backup
```

### 5.2 创建应用配置

```bash
sudo nano /etc/nginx/sites-available/raspberrycloud
```

基础配置（HTTP，后续会升级到HTTPS）：

```nginx
# 上游后端服务
upstream backend {
    server 127.0.0.1:8000;
}

# HTTP服务器
server {
    listen 80;
    listen [::]:80;
    server_name _;  # 后续替换为你的域名

    # 客户端最大请求体（允许大文件上传）
    client_max_body_size 10G;
    client_body_buffer_size 128k;
    client_body_timeout 3600s;
    
    # 代理超时设置
    proxy_connect_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_read_timeout 3600s;

    # 前端静态文件
    location / {
        root /var/www/raspberrycloud;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # 静态资源缓存
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2)$ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }

    # API代理到后端
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 支持大文件上传
        proxy_request_buffering off;
    }

    # WebSocket代理（文件同步）
    location /ws/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 日志
    access_log /var/log/nginx/raspberrycloud_access.log;
    error_log /var/log/nginx/raspberrycloud_error.log;
}
```

### 5.3 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/raspberrycloud /etc/nginx/sites-enabled/

# 删除默认配置
sudo rm /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 预期输出：
# nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
# nginx: configuration file /etc/nginx/nginx.conf test is successful

# 重启Nginx
sudo systemctl restart nginx
```

## 🚀 步骤6：配置系统服务（开机自启）

### 6.1 创建systemd服务文件

```bash
sudo nano /etc/systemd/system/raspberrycloud.service
```

内容：

```ini
[Unit]
Description=RaspberryCloud Private Cloud Storage Service
After=network.target mysql.service

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/raspberrycloud/backend
Environment="PATH=/opt/raspberrycloud/venv/bin"
ExecStart=/opt/raspberrycloud/venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000 --workers 2

# 自动重启
Restart=always
RestartSec=10

# 资源限制
LimitNOFILE=65536
MemoryLimit=512M

# 日志
StandardOutput=append:/var/log/raspberrycloud/backend.log
StandardError=append:/var/log/raspberrycloud/backend_error.log

[Install]
WantedBy=multi-user.target
```

### 6.2 创建日志目录

```bash
sudo mkdir -p /var/log/raspberrycloud
sudo chown -R www-data:www-data /var/log/raspberrycloud
```

### 6.3 启动服务

```bash
# 重新加载systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start raspberrycloud

# 检查状态
sudo systemctl status raspberrycloud

# 预期输出：
# ● raspberrycloud.service - RaspberryCloud Private Cloud Storage Service
#    Loaded: loaded (/etc/systemd/system/raspberrycloud.service; disabled)
#    Active: active (running) since ...

# 设置开机自启
sudo systemctl enable raspberrycloud
```

### 6.4 管理服务命令

```bash
# 启动
sudo systemctl start raspberrycloud

# 停止
sudo systemctl stop raspberrycloud

# 重启
sudo systemctl restart raspberrycloud

# 查看状态
sudo systemctl status raspberrycloud

# 查看日志
sudo journalctl -u raspberrycloud -f  # 实时日志
sudo journalctl -u raspberrycloud --since "10 minutes ago"  # 最近10分钟
tail -f /var/log/raspberrycloud/backend.log  # 应用日志
```

## 📁 步骤7：配置Samba（局域网共享）

### 7.1 配置Samba

```bash
# 备份配置
sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup

# 编辑配置
sudo nano /etc/samba/smb.conf
```

在文件末尾添加：

```ini
[cloud]
   comment = RaspberryCloud Storage
   path = /mnt/cloud_storage/users
   browseable = yes
   writable = yes
   valid users = @cloud-users
   create mask = 0664
   directory mask = 0775
   force user = www-data
   force group = www-data
```

### 7.2 创建Samba用户

```bash
# 创建系统用户组
sudo groupadd cloud-users

# 添加www-data到组
sudo usermod -aG cloud-users www-data

# 为管理员创建Samba密码
sudo smbpasswd -a $USER
# 输入Samba密码（可与系统密码不同）

# 添加用户到cloud-users组
sudo usermod -aG cloud-users $USER
```

### 7.3 重启Samba

```bash
# 测试配置
testparm

# 重启服务
sudo systemctl restart smbd
sudo systemctl restart nmbd
```

### 7.4 测试Samba访问

**Windows访问：**
- 打开文件资源管理器
- 地址栏输入：`\\树莓派IP\cloud`
- 输入用户名和密码

**Mac访问：**
- Finder → 前往 → 连接服务器
- 输入：`smb://树莓派IP/cloud`

**Linux访问：**
```bash
smbclient //树莓派IP/cloud -U 用户名
```

## 🧪 步骤8：全面测试

### 8.1 测试后端API

```bash
# 健康检查
curl http://localhost/api/health

# 测试登录
curl -X POST http://localhost/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"RaspberryCloud2024!"}'

# 应返回JWT token
```

### 8.2 测试Web界面

1. 浏览器访问：`http://树莓派IP`
2. 应该看到登录页面
3. 使用默认账户登录：
   - 用户名：admin
   - 密码：RaspberryCloud2024!
4. 测试功能：
   - 上传小文件（<10MB）
   - 创建文件夹
   - 文件重命名
   - 文件删除
   - 文件预览

### 8.3 性能测试

```bash
# 查看系统资源占用
htop

# 查看内存使用
free -h

# 查看磁盘I/O
iostat -x 1

# 查看网络连接
ss -tunlp | grep -E '(8000|80|443)'
```

## 📊 步骤9：监控与日志

### 9.1 设置日志轮转

```bash
sudo nano /etc/logrotate.d/raspberrycloud
```

内容：

```
/var/log/raspberrycloud/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 www-data www-data
    sharedscripts
    postrotate
        systemctl reload raspberrycloud > /dev/null 2>&1 || true
    endscript
}
```

### 9.2 创建监控脚本（可选）

```bash
sudo nano /opt/raspberrycloud/scripts/monitor.sh
```

内容：

```bash
#!/bin/bash
# RaspberryCloud监控脚本

echo "=== RaspberryCloud状态 ==="
echo "时间: $(date)"
echo ""

# 服务状态
echo "后端服务:"
systemctl is-active raspberrycloud

echo "Nginx状态:"
systemctl is-active nginx

echo ""
echo "=== 资源使用 ==="
echo "内存:"
free -h | grep Mem

echo "磁盘:"
df -h | grep -E '(cloud_storage|/$)'

echo "CPU温度:"
vcgencmd measure_temp

echo ""
echo "=== 活动连接 ==="
ss -tunlp | grep -E '(8000|80|443)' | wc -l
```

设置可执行：

```bash
chmod +x /opt/raspberrycloud/scripts/monitor.sh
```

运行监控：

```bash
/opt/raspberrycloud/scripts/monitor.sh
```

### 9.3 设置定时任务

```bash
crontab -e
```

添加：

```bash
# 每小时清理临时文件
0 * * * * find /mnt/cloud_storage/temp -type f -mtime +1 -delete

# 每天凌晨备份数据库
0 3 * * * /opt/raspberrycloud/scripts/backup.sh

# 每5分钟检查服务状态
*/5 * * * * systemctl is-active raspberrycloud || systemctl restart raspberrycloud
```

## ✅ 部署完成检查清单

- [ ] Python虚拟环境已创建并激活
- [ ] 所有Python依赖已安装
- [ ] 数据库已初始化
- [ ] 后端服务可以启动（systemctl status raspberrycloud）
- [ ] Nginx配置正确（nginx -t）
- [ ] Web界面可访问（http://树莓派IP）
- [ ] 可以登录默认管理员账户
- [ ] 文件上传/下载功能正常
- [ ] Samba共享可访问
- [ ] 服务已设置开机自启
- [ ] 日志正常输出

## 🎉 下一步

系统部署完成！接下来：
1. [配置多端访问](03-多端访问配置.md) - 设置外网访问、HTTPS、移动端
2. [安全加固](04-安全加固指南.md) - 增强系统安全性
3. 修改默认管理员密码！

## 🔧 故障排除

### 后端服务无法启动

```bash
# 查看详细日志
sudo journalctl -u raspberrycloud -n 50 --no-pager

# 常见问题：
# 1. 端口被占用
sudo lsof -i :8000
# 杀死进程：sudo kill -9 PID

# 2. Python依赖缺失
source /opt/raspberrycloud/venv/bin/activate
pip install -r /opt/raspberrycloud/backend/requirements.txt

# 3. 数据库连接失败
# 检查.env配置的DATABASE_URL是否正确
```

### Nginx 502错误

```bash
# 检查后端是否运行
sudo systemctl status raspberrycloud

# 检查端口
curl http://localhost:8000/api/health

# 查看Nginx日志
sudo tail -f /var/log/nginx/raspberrycloud_error.log
```

### 文件上传失败

```bash
# 检查存储目录权限
ls -la /mnt/cloud_storage/

# 修复权限
sudo chown -R www-data:www-data /mnt/cloud_storage
sudo chmod -R 755 /mnt/cloud_storage

# 检查磁盘空间
df -h /mnt/cloud_storage
```


