# 硬件准备与初始化

本文档详细说明树莓派系统烧录、外接存储挂载、SSH开启、网络配置等初始化步骤。

## 📦 硬件清单

### 必备硬件

| 项目 | 规格 | 参考价格 | 说明 |
|------|------|---------|------|
| 树莓派4B/5 | 4GB/8GB | ¥400-800 | 推荐8GB内存版本 |
| 电源适配器 | 5V/3A USB-C | ¥50-80 | 官方电源更稳定 |
| microSD卡 | 64GB Class 10 | ¥60-100 | 系统盘，推荐SanDisk |
| 外接硬盘 | 1TB USB 3.0 | ¥300-500 | 数据存储，推荐WD/希捷 |
| 散热片/风扇 | 适配型号 | ¥20-50 | 长时间运行必备 |

### 可选硬件

- **硬盘盒**：支持2.5/3.5寸SATA硬盘，USB 3.0接口
- **UPS电源**：防止突然断电导致数据损坏
- **网线**：千兆网线，比WiFi更稳定
- **外壳**：带散热孔的亚克力/金属外壳

## 🔧 步骤1：系统烧录

### 方法一：使用Raspberry Pi Imager（推荐）

1. **下载工具**
   - 访问：https://www.raspberrypi.com/software/
   - 下载适合你操作系统的版本（Windows/Mac/Linux）

2. **烧录系统**
   ```
   - 插入microSD卡到电脑
   - 打开Raspberry Pi Imager
   - 选择操作系统：
     * Raspberry Pi OS (64-bit) - 推荐
     * 或 Raspberry Pi OS Lite (64-bit) - 无桌面版，更省资源
   - 选择存储卡：你的microSD卡
   - 点击"设置"齿轮图标（右下角）
   ```

3. **高级设置（重要！）**
   ```
   ✅ 启用SSH
      - 选择"使用密码验证"
   
   ✅ 设置用户名和密码
      - 用户名：pi (或自定义)
      - 密码：设置强密码（至少8位，含大小写+数字）
   
   ✅ 配置无线局域网（可选）
      - SSID：你的WiFi名称
      - 密码：WiFi密码
      - 国家：CN
   
   ✅ 设置时区和键盘布局
      - 时区：Asia/Shanghai
      - 键盘布局：cn
   ```

4. **开始烧录**
   - 点击"写入"
   - 等待完成（约5-10分钟）
   - 完成后安全弹出SD卡

### 方法二：使用命令行（Linux/Mac）

```bash
# 1. 下载系统镜像
wget https://downloads.raspberrypi.org/raspios_arm64/images/raspios_arm64-2024-11-19/2024-11-19-raspios-bookworm-arm64.img.xz

# 2. 解压镜像
xz -d 2024-11-19-raspios-bookworm-arm64.img.xz

# 3. 查看SD卡设备名
lsblk  # 找到你的SD卡，如 /dev/sdb

# 4. 烧录镜像（替换/dev/sdX为你的SD卡）
sudo dd if=2024-11-19-raspios-bookworm-arm64.img of=/dev/sdX bs=4M status=progress
sudo sync

# 5. 启用SSH（在boot分区创建ssh文件）
touch /media/你的用户名/bootfs/ssh
```

## 🌐 步骤2：首次启动与连接

### 硬件连接

1. 将烧录好的SD卡插入树莓派
2. 连接外接硬盘到USB 3.0口（蓝色接口）
3. 连接网线（推荐）或使用WiFi
4. **最后**连接电源（树莓派会自动开机）

### 查找树莓派IP地址

**方法一：路由器管理页面**
- 登录路由器后台（通常是192.168.1.1或192.168.0.1）
- 查看已连接设备列表，找到"raspberrypi"

**方法二：使用nmap扫描**
```bash
# 安装nmap
sudo apt install nmap  # Linux
brew install nmap      # Mac

# 扫描局域网
nmap -sn 192.168.1.0/24  # 替换为你的网段

# 找到类似这样的条目：
# Nmap scan report for raspberrypi.local (192.168.1.100)
```

**方法三：连接显示器**
- 用HDMI线连接显示器
- 启动后终端会显示IP地址

### SSH连接

```bash
# Windows用户使用PowerShell或安装Git Bash
# Mac/Linux直接使用终端

ssh pi@树莓派IP地址
# 例如：ssh pi@192.168.1.100

# 首次连接会提示确认指纹，输入 yes
# 输入密码（你在Imager中设置的密码）
```

## ⚙️ 步骤3：系统初始化配置

### 3.1 更新系统

```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包（可能需要10-20分钟）
sudo apt upgrade -y

# 安装常用工具
sudo apt install -y vim git curl wget htop ufw
```

### 3.2 系统配置（raspi-config）

```bash
sudo raspi-config
```

推荐配置项：

```
1. System Options
   → S4 Hostname: 修改主机名为 "raspberrycloud"
   → S5 Boot / Auto Login: 选择 "Console" (命令行模式，省资源)

3. Interface Options
   → I1 SSH: Enable (如果未开启)
   → I2 VNC: Disable (不需要图形界面)

4. Performance Options
   → P2 GPU Memory: 设置为 16 (降低GPU内存，服务器无需高GPU)

5. Localisation Options
   → L1 Locale: en_US.UTF-8 UTF-8 (保持英文，兼容性好)
   → L2 Timezone: Asia/Shanghai
   → L4 WLAN Country: CN

6. Advanced Options
   → A1 Expand Filesystem: 自动扩展SD卡分区
```

配置完成后选择 `Finish`，选择 `Yes` 重启。

### 3.3 设置静态IP（重要！）

#### 方法一：编辑dhcpcd.conf（推荐）

```bash
sudo nano /etc/dhcpcd.conf
```

在文件末尾添加（根据你的网络环境修改）：

```bash
# 静态IP配置
interface eth0  # 有线网络，WiFi改为wlan0
static ip_address=192.168.1.100/24  # 你想要的静态IP
static routers=192.168.1.1          # 你的路由器IP
static domain_name_servers=192.168.1.1 8.8.8.8
```

保存退出（Ctrl+O，回车，Ctrl+X），重启网络：

```bash
sudo systemctl restart dhcpcd
```

#### 方法二：使用nmcli（Bookworm新版本）

```bash
# 查看当前连接
nmcli con show

# 设置静态IP（替换连接名称）
sudo nmcli con mod "Wired connection 1" \
  ipv4.addresses 192.168.1.100/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.dns "192.168.1.1 8.8.8.8" \
  ipv4.method manual

# 重启连接
sudo nmcli con down "Wired connection 1"
sudo nmcli con up "Wired connection 1"
```

### 3.4 关闭不必要的服务（性能优化）

```bash
# 禁用蓝牙（服务器不需要）
sudo systemctl disable bluetooth
sudo systemctl stop bluetooth

# 禁用音频（服务器不需要）
sudo systemctl disable alsa-state

# 禁用图形界面（如果安装了桌面版）
sudo systemctl set-default multi-user.target

# 查看占用资源的服务
systemctl list-units --type=service --state=running
```

### 3.5 配置SWAP（增加虚拟内存）

```bash
# 检查当前SWAP大小
free -h

# 编辑SWAP配置
sudo nano /etc/dphys-swapfile

# 修改这一行（2GB内存设置1GB SWAP，4GB以上可设置2GB）
CONF_SWAPSIZE=1024  # 单位MB

# 重启SWAP服务
sudo /etc/init.d/dphys-swapfile stop
sudo /etc/init.d/dphys-swapfile start
```

## 💾 步骤4：外接存储挂载

### 4.1 识别外接硬盘

```bash
# 查看所有存储设备
lsblk

# 输出示例：
# NAME        MAJ:MIN RM   SIZE RO TYPE MOUNTPOINTS
# sda           8:0    0 931.5G  0 disk        ← 你的外接硬盘
# └─sda1        8:1    0 931.5G  0 part
# mmcblk0     179:0    0  59.5G  0 disk
# ├─mmcblk0p1 179:1    0   512M  0 part /boot/firmware
# └─mmcblk0p2 179:2    0    59G  0 part /

# 查看分区详细信息
sudo fdisk -l /dev/sda
```

### 4.2 格式化硬盘（仅新硬盘需要）

⚠️ **警告**：格式化会删除硬盘所有数据！

```bash
# 如果硬盘已有数据，跳过此步骤
# 如果是新硬盘或需要格式化：

# 创建分区表
sudo fdisk /dev/sda
# 输入命令：
# n (新建分区)
# p (主分区)
# 1 (分区号)
# 回车 (默认起始扇区)
# 回车 (默认结束扇区，使用全部空间)
# w (写入并退出)

# 格式化为ext4（Linux推荐）
sudo mkfs.ext4 /dev/sda1

# 或格式化为exFAT（跨平台兼容）
sudo apt install exfat-fuse exfat-utils
sudo mkfs.exfat /dev/sda1
```

### 4.3 创建挂载点并挂载

```bash
# 创建挂载目录
sudo mkdir -p /mnt/cloud_storage

# 临时挂载测试
sudo mount /dev/sda1 /mnt/cloud_storage

# 检查是否成功
df -h | grep cloud_storage

# 设置权限（允许普通用户读写）
sudo chown -R pi:pi /mnt/cloud_storage
sudo chmod -R 755 /mnt/cloud_storage
```

### 4.4 设置开机自动挂载

```bash
# 获取硬盘UUID（比设备名更稳定）
sudo blkid /dev/sda1

# 输出示例：
# /dev/sda1: UUID="1234-5678-90AB-CDEF" TYPE="ext4"

# 备份fstab文件
sudo cp /etc/fstab /etc/fstab.backup

# 编辑fstab
sudo nano /etc/fstab

# 在文件末尾添加（替换UUID为你的实际UUID）：
UUID=1234-5678-90AB-CDEF /mnt/cloud_storage ext4 defaults,nofail 0 2

# 参数说明：
# defaults: 默认挂载选项
# nofail: 即使硬盘未连接也能正常启动系统
# 0: 不备份
# 2: 开机检查优先级
```

### 4.5 测试自动挂载

```bash
# 卸载硬盘
sudo umount /mnt/cloud_storage

# 测试fstab配置
sudo mount -a

# 检查是否成功
df -h | grep cloud_storage

# 重启测试
sudo reboot
# 重新SSH连接后检查
df -h | grep cloud_storage
```

### 4.6 创建存储目录结构

```bash
# 创建应用目录结构
sudo mkdir -p /mnt/cloud_storage/{users,shares,backups,temp}

# users: 用户文件存储
# shares: 分享文件临时存储
# backups: 备份文件
# temp: 临时文件（上传缓存）

# 设置权限
sudo chown -R www-data:www-data /mnt/cloud_storage
sudo chmod -R 755 /mnt/cloud_storage
```

## 🔒 步骤5：安全基础配置

### 5.1 创建普通用户（禁止root直接登录）

```bash
# 如果还没有创建普通用户
sudo adduser cloud
# 按提示设置密码

# 添加到sudo组
sudo usermod -aG sudo cloud

# 切换到新用户测试
su - cloud
sudo ls  # 测试sudo权限
```

### 5.2 配置SSH安全

```bash
sudo nano /etc/ssh/sshd_config
```

修改以下配置：

```bash
# 禁止root登录
PermitRootLogin no

# 禁用密码登录（可选，建议配置密钥后再开启）
# PasswordAuthentication no

# 修改SSH端口（可选，降低被扫描风险）
# Port 2222

# 仅允许特定用户登录
AllowUsers pi cloud
```

重启SSH服务：

```bash
sudo systemctl restart ssh
```

### 5.3 配置防火墙（UFW）

```bash
# 启用UFW
sudo ufw enable

# 允许SSH（如果修改了端口，替换22为新端口）
sudo ufw allow 22/tcp

# 允许HTTP和HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 查看规则
sudo ufw status verbose

# 开机自启
sudo systemctl enable ufw
```

## 📊 步骤6：验证系统状态

### 运行验证脚本

```bash
# 查看系统信息
uname -a

# 查看内存使用
free -h

# 查看存储空间
df -h

# 查看CPU温度
vcgencmd measure_temp

# 查看系统负载
htop  # 按q退出
```

### 预期结果

```
✅ 系统版本：Linux 6.x aarch64
✅ 可用内存：>3GB (4GB版本) 或 >7GB (8GB版本)
✅ SD卡空闲：>20GB
✅ 外接存储：已挂载到 /mnt/cloud_storage
✅ CPU温度：<60°C (空闲时)
✅ 静态IP：已设置且可访问
```

## 🎉 完成！

硬件和系统初始化完成，接下来进入[系统部署教程](02-系统部署教程.md)。

## 🔧 常见问题

### Q1: SSH无法连接
```bash
# 检查SSH服务状态
sudo systemctl status ssh

# 重启SSH服务
sudo systemctl restart ssh

# 检查防火墙是否阻止
sudo ufw status
```

### Q2: 外接硬盘无法挂载
```bash
# 检查硬盘是否识别
lsblk

# 检查文件系统类型
sudo blkid /dev/sda1

# 手动挂载测试
sudo mount -t ext4 /dev/sda1 /mnt/cloud_storage

# 查看系统日志
dmesg | tail -20
```

### Q3: WiFi连接不稳定
- 建议使用有线网络，更稳定
- 检查WiFi信号强度：`iwconfig`
- 尝试切换到5GHz频段

### Q4: 系统运行缓慢
```bash
# 检查是否在更新
ps aux | grep apt

# 检查内存占用
free -h

# 检查磁盘I/O
iotop  # 需要安装：sudo apt install iotop
```


