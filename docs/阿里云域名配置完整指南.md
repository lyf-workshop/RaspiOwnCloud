# 阿里云域名配置完整指南

从购买域名到通过域名访问网站的完整流程。

## 📋 第一步：在阿里云购买域名

### 1.1 注册/登录阿里云账号

```
1. 访问 https://www.aliyun.com
2. 点击右上角"登录"或"注册"
3. 完成账号注册/登录
4. 完成实名认证（购买域名需要）
```

### 1.2 购买域名

```
1. 登录后，在顶部搜索框输入"域名"
2. 点击"域名注册"或访问 https://wanwang.aliyun.com
3. 在搜索框输入想要的域名（如: mycloud）
4. 选择域名后缀（推荐: .com, .cn, .top）
5. 点击"加入清单"
6. 选择购买年限（建议1-3年）
7. 点击"立即结算"
8. 填写域名信息（域名所有者信息）
9. 选择支付方式并完成支付
```

**域名选择建议：**
- `.com` - 国际通用，价格较高（约50-100元/年）
- `.cn` - 中国域名，需要备案（约30-50元/年）
- `.top` - 价格便宜（约10-20元/年）
- `.xyz` - 价格便宜（约10-20元/年）

### 1.3 完成域名实名认证

```
1. 购买后，进入"域名控制台"
2. 找到购买的域名
3. 点击"实名认证"
4. 上传身份证照片
5. 等待审核（通常1-3个工作日）
```

## 🌐 第二步：配置域名解析

### 2.1 获取树莓派公网IP

在树莓派上执行：

```bash
# 获取当前公网IP
curl ip.sb

# 或
curl ifconfig.me

# 记录这个IP地址，例如: 123.45.67.89
```

**重要：**
- 如果IP是动态的（每次重启会变化），需要配置DDNS
- 如果IP是固定的，直接配置A记录即可

### 2.2 在阿里云配置DNS解析

#### 步骤1：进入域名解析控制台

```
1. 登录阿里云控制台
2. 点击"产品与服务" → "域名"
3. 点击"域名解析DNS"
4. 或直接访问: https://dns.console.aliyun.com
```

#### 步骤2：添加解析记录

```
1. 找到你购买的域名
2. 点击"解析设置"
3. 点击"添加记录"
```

**配置A记录（主域名）：**

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | @ | 你的公网IP | 600 |

**配置A记录（www子域名）：**

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | www | 你的公网IP | 600 |

**示例：**
- 域名：`mycloud.com`
- 公网IP：`123.45.67.89`
- 配置后：
  - `mycloud.com` → `123.45.67.89`
  - `www.mycloud.com` → `123.45.67.89`

#### 步骤3：保存并等待生效

```
1. 点击"确认"
2. 等待DNS解析生效（通常5-30分钟）
3. 验证解析：在命令行执行
   nslookup mycloud.com
   应该返回你的公网IP
```

## 🔧 第三步：配置树莓派

### 3.1 修改Nginx配置

```bash
# 编辑Nginx配置
sudo nano /etc/nginx/sites-available/raspberrycloud
```

修改 `server_name`：

```nginx
server_name mycloud.com www.mycloud.com;
```

**完整配置示例：**

```nginx
# HTTP服务器
server {
    listen 80;
    listen [::]:80;
    server_name mycloud.com www.mycloud.com;  # 替换为你的域名

    # Let's Encrypt验证
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 其他配置保持不变...
}
```

```bash
# 测试配置
sudo nginx -t

# 重新加载Nginx
sudo systemctl reload nginx
```

### 3.2 配置路由器端口转发

**重要：** 如果运营商封禁80/443端口，需要映射到其他端口。

#### 方案A：使用标准端口（80/443）

**路由器配置：**

| 规则名称 | 外部端口 | 内部IP | 内部端口 | 协议 |
|---------|---------|--------|---------|------|
| HTTP | 80 | 树莓派IP | 80 | TCP |
| HTTPS | 443 | 树莓派IP | 443 | TCP |

#### 方案B：使用非标准端口（如果80/443被封）

**路由器配置：**

| 规则名称 | 外部端口 | 内部IP | 内部端口 | 协议 |
|---------|---------|--------|---------|------|
| HTTP | 8080 | 树莓派IP | 80 | TCP |
| HTTPS | 8443 | 树莓派IP | 443 | TCP |

**如果使用非标准端口，访问时需要加端口：**
- `http://mycloud.com:8080`
- `https://mycloud.com:8443`

### 3.3 更新防火墙规则

```bash
# 如果使用标准端口
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 如果使用非标准端口
sudo ufw allow 8080/tcp
sudo ufw allow 8443/tcp
```

## 🔒 第四步：配置HTTPS（SSL证书）

### 4.1 安装Certbot

```bash
sudo apt update
sudo apt install -y certbot python3-certbot-nginx
```

### 4.2 申请SSL证书

#### 如果使用标准端口（80/443）

```bash
# 使用nginx插件自动配置（推荐）
sudo certbot --nginx -d mycloud.com -d www.mycloud.com

# 按提示操作：
# 1. 输入邮箱地址（用于证书到期提醒）
# 2. 同意服务条款 (A)
# 3. 是否接收邮件通知 (N)
# 4. 是否重定向HTTP到HTTPS (2 - 推荐选择)
```

#### 如果使用非标准端口（8080/8443）

**方法1：临时使用80端口申请证书**

```bash
# 1. 临时修改路由器，将80端口映射到树莓派
# 2. 申请证书
sudo certbot --nginx -d mycloud.com -d www.mycloud.com

# 3. 申请完成后，改回8080端口映射
```

**方法2：使用standalone模式**

```bash
# 1. 临时停止Nginx
sudo systemctl stop nginx

# 2. 申请证书（需要80端口可用）
sudo certbot certonly --standalone -d mycloud.com -d www.mycloud.com

# 3. 启动Nginx
sudo systemctl start nginx

# 4. 手动配置Nginx使用证书（见下一步）
```

### 4.3 配置Nginx使用SSL证书

如果使用standalone模式申请证书，需要手动配置：

```bash
sudo nano /etc/nginx/sites-available/raspberrycloud
```

取消HTTPS server块的注释，并修改：

```nginx
# HTTPS服务器
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name mycloud.com www.mycloud.com;  # 你的域名

    # SSL证书配置
    ssl_certificate /etc/letsencrypt/live/mycloud.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mycloud.com/privkey.pem;
    
    # SSL优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # 安全头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 客户端最大请求体
    client_max_body_size 10G;
    client_body_buffer_size 128k;
    client_body_timeout 3600s;
    
    # 代理超时设置
    proxy_connect_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_read_timeout 3600s;

    # 前端静态文件
    location / {
        root /var/www/raspberrycloud;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # 静态资源缓存
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2)$ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }

    # API代理
    location /api/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        
        # 支持大文件上传
        proxy_request_buffering off;
    }

    # WebSocket代理
    location /ws/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket超时
        proxy_read_timeout 86400;
    }

    # 日志
    access_log /var/log/nginx/raspberrycloud_ssl_access.log;
    error_log /var/log/nginx/raspberrycloud_ssl_error.log;
}
```

**HTTP自动重定向到HTTPS：**

在HTTP server块中添加：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name mycloud.com www.mycloud.com;
    
    # 重定向到HTTPS
    return 301 https://$server_name$request_uri;
}
```

```bash
# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

### 4.4 设置证书自动续期

```bash
# 测试续期
sudo certbot renew --dry-run

# 设置自动续期（每月1日凌晨3点）
sudo crontab -e
# 添加：
0 3 1 * * certbot renew --quiet && systemctl reload nginx
```

## 🚀 第五步：完整部署流程（一键脚本）

创建自动化部署脚本：

```bash
# 在树莓派上执行
cd ~/Desktop/Github/RaspiOwnCloud
chmod +x scripts/setup_external_access.sh
sudo bash scripts/setup_external_access.sh
```

## ✅ 第六步：验证配置

### 6.1 验证域名解析

```bash
# 在树莓派或本地电脑上执行
nslookup mycloud.com

# 应该返回你的公网IP
# 例如: 123.45.67.89
```

### 6.2 验证端口转发

```bash
# 从外网测试（使用手机4G或在线工具）
curl http://mycloud.com/api/health

# 或
curl https://mycloud.com/api/health
```

### 6.3 验证HTTPS证书

```
1. 浏览器访问: https://mycloud.com
2. 检查地址栏是否有锁图标 🔒
3. 点击锁图标查看证书信息
4. 确认证书有效
```

## 📱 访问方式

配置完成后，可以通过以下方式访问：

- **HTTPS（推荐）**: `https://mycloud.com`
- **HTTP（自动跳转）**: `http://mycloud.com` → 自动跳转到HTTPS
- **带www**: `https://www.mycloud.com`

## 🔧 如果使用非标准端口

如果运营商封禁80/443端口，使用8080/8443：

### 修改Nginx配置

```bash
sudo nano /etc/nginx/sites-available/raspberrycloud
```

修改HTTPS监听端口：

```nginx
server {
    listen 8443 ssl http2;  # 改为8443
    # ... 其他配置
}
```

### 访问方式

- `https://mycloud.com:8443`
- `http://mycloud.com:8080`

## 🐛 常见问题

### Q1: 域名解析不生效

**检查：**
```bash
# 检查DNS解析
nslookup mycloud.com
dig mycloud.com

# 如果返回的不是你的公网IP，等待DNS传播（最多48小时）
```

**解决方案：**
1. 检查阿里云解析记录是否正确
2. 清除本地DNS缓存
3. 等待DNS传播

### Q2: SSL证书申请失败

**错误：** `Failed to verify domain ownership`

**原因：**
- 域名未正确解析到公网IP
- 80端口未开放或未映射

**解决方案：**
```bash
# 1. 验证域名解析
nslookup mycloud.com

# 2. 检查80端口是否可访问
curl http://mycloud.com

# 3. 如果使用非标准端口，临时映射80端口申请证书
```

### Q3: 访问显示"不安全连接"

**原因：**
- 证书未正确配置
- 证书过期

**解决方案：**
```bash
# 检查证书
sudo certbot certificates

# 重新申请证书
sudo certbot --nginx -d mycloud.com -d www.mycloud.com --force-renewal
```

### Q4: 公网IP变化（动态IP）

**解决方案：配置DDNS自动更新**

```bash
# 使用阿里云DNS API自动更新
# 需要获取AccessKey和SecretKey
```

## 📋 完整检查清单

- [ ] 阿里云域名已购买并实名认证
- [ ] 域名解析记录已配置（A记录）
- [ ] 路由器端口转发已配置
- [ ] Nginx配置已更新（server_name）
- [ ] SSL证书已申请
- [ ] HTTPS配置已启用
- [ ] 防火墙规则已更新
- [ ] 域名解析已生效（nslookup验证）
- [ ] 外网访问测试成功
- [ ] HTTPS访问正常（有锁图标）

## 🎉 完成！

现在你可以通过 `https://mycloud.com` 从任何地方访问你的私有云存储了！

**安全提示：**
- 立即修改默认管理员密码
- 定期更新系统和软件
- 定期检查访问日志
- 备份重要数据








