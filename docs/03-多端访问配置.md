# 多端访问配置

本文档介绍如何配置局域网访问、外网访问、HTTPS加密和移动端适配。

## 📱 访问方式概览

| 访问方式 | 地址 | 特点 | 适用场景 |
|---------|------|------|---------|
| 局域网Web | http://树莓派IP | 快速，无需配置 | 家庭/办公室内 |
| 局域网磁盘挂载 | \\树莓派IP\cloud | 像本地磁盘 | 频繁访问 |
| 外网Web | https://你的域名 | 随时随地访问 | 远程办公 |
| 移动端PWA | 浏览器添加到桌面 | 类原生APP | 手机使用 |

## 🏠 方式一：局域网Web访问

### 1.1 电脑浏览器访问

**Windows/Mac/Linux通用：**

```
1. 打开浏览器（推荐Chrome/Firefox/Edge）
2. 地址栏输入: http://树莓派IP
   例如: http://192.168.1.100
3. 输入用户名和密码登录
```

**查找树莓派IP地址：**

```bash
# 在树莓派上运行
hostname -I

# 或从路由器管理页面查看已连接设备
```

### 1.2 手机浏览器访问

```
1. 连接到同一WiFi网络
2. 打开浏览器
3. 输入: http://树莓派IP
4. 点击右上角菜单 → "添加到主屏幕"（PWA）
```

## 💾 方式二：局域网磁盘挂载

### 2.1 Windows系统挂载

#### 方法一：文件资源管理器

```
1. 打开"此电脑"
2. 点击顶部"计算机" → "映射网络驱动器"
3. 驱动器: 选择一个盘符（如Z:）
4. 文件夹: \\树莓派IP\cloud
   例如: \\192.168.1.100\cloud
5. 勾选"登录时重新连接"
6. 点击"完成"
7. 输入Samba用户名和密码
```

#### 方法二：命令行

```cmd
# 打开命令提示符
net use Z: \\192.168.1.100\cloud /user:你的用户名 你的密码 /persistent:yes

# 断开映射
net use Z: /delete
```

### 2.2 Mac系统挂载

```
1. Finder → "前往" → "连接服务器"（⌘+K）
2. 服务器地址: smb://树莓派IP/cloud
   例如: smb://192.168.1.100/cloud
3. 点击"连接"
4. 选择"注册用户"
5. 输入用户名和密码
6. 勾选"在我的钥匙串中记住此密码"（可选）
7. 点击"连接"

# 设置开机自动挂载
系统偏好设置 → 用户与群组 → 登录项 → 添加挂载的网络驱动器
```

### 2.3 Linux系统挂载

#### 临时挂载

```bash
# 安装cifs-utils
sudo apt install cifs-utils  # Debian/Ubuntu
sudo yum install cifs-utils  # CentOS/RHEL

# 创建挂载点
sudo mkdir -p /mnt/raspberrycloud

# 挂载
sudo mount -t cifs //树莓派IP/cloud /mnt/raspberrycloud -o username=你的用户名,password=你的密码

# 卸载
sudo umount /mnt/raspberrycloud
```

#### 永久挂载（开机自动）

```bash
# 创建凭据文件
sudo nano /root/.smbcredentials

# 添加内容：
username=你的用户名
password=你的密码

# 设置权限
sudo chmod 600 /root/.smbcredentials

# 编辑fstab
sudo nano /etc/fstab

# 添加一行：
//树莓派IP/cloud /mnt/raspberrycloud cifs credentials=/root/.smbcredentials,iocharset=utf8,uid=1000,gid=1000 0 0

# 测试挂载
sudo mount -a
```

## 🌐 方式三：外网访问配置

### 3.1 前置条件检查

```bash
# 检查路由器是否有公网IP
curl ip.sb

# 如果输出的IP与路由器WAN口IP一致，则有公网IP
# 登录路由器查看WAN口IP进行对比
```

### 3.2 配置端口映射

#### 光猫桥接模式（推荐）

```
1. 联系运营商申请光猫改为桥接模式
2. 路由器拨号上网（PPPoE）
3. 路由器管理页面 → 端口转发/虚拟服务器
4. 添加规则：
   - 外部端口: 8080（或其他未占用端口）
   - 内部IP: 树莓派IP（如192.168.1.100）
   - 内部端口: 80
   - 协议: TCP
5. 添加HTTPS规则（配置SSL后）：
   - 外部端口: 8443
   - 内部IP: 树莓派IP
   - 内部端口: 443
   - 协议: TCP
```

#### 路由器直接映射

```
1. 登录路由器管理页面（通常是192.168.1.1或192.168.0.1）
2. 找到"端口转发"/"虚拟服务器"/"NAT"设置
3. 添加规则（同上）
```

**常见路由器配置位置：**
- TP-Link: 应用管理 → 虚拟服务器
- 华为: 安全设置 → 虚拟服务器
- 小米: 高级设置 → 端口转发

### 3.3 动态DNS（DDNS）配置

如果没有固定公网IP，需要配置DDNS：

#### 方法一：使用路由器内置DDNS

```
1. 路由器管理页面 → 动态DNS
2. 选择服务提供商（如花生壳、No-IP）
3. 注册账号并绑定域名
4. 路由器输入账号密码
5. 保存并等待更新
```

#### 方法二：在树莓派上配置DDNS

**使用花生壳：**

```bash
# 下载花生壳客户端
wget https://hsk.oray.com/download/phddns_5.3.0_aarch64.deb

# 安装
sudo dpkg -i phddns_5.3.0_aarch64.deb

# 启动
sudo phddns start

# 查看SN码
sudo phddns status

# 在花生壳官网使用SN码激活
```

**使用No-IP：**

```bash
# 下载客户端
cd /usr/local/src
sudo wget https://www.noip.com/client/linux/noip-duc-linux.tar.gz
sudo tar xf noip-duc-linux.tar.gz
cd noip-2.1.9-1

# 编译安装
sudo make install

# 配置（输入No-IP账号和密码）
sudo /usr/local/bin/noip2 -C

# 启动
sudo /usr/local/bin/noip2

# 设置开机自启
sudo nano /etc/systemd/system/noip2.service
```

noip2.service内容：

```ini
[Unit]
Description=No-IP Dynamic DNS Update Client
After=network.target

[Service]
Type=forking
ExecStart=/usr/local/bin/noip2

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable noip2
sudo systemctl start noip2
```

### 3.4 配置HTTPS（Let's Encrypt）

#### 3.4.1 安装Certbot

```bash
sudo apt install certbot python3-certbot-nginx
```

#### 3.4.2 修改Nginx配置

```bash
sudo nano /etc/nginx/sites-available/raspberrycloud
```

修改`server_name`为你的域名：

```nginx
server_name your-domain.com www.your-domain.com;
```

```bash
sudo nginx -t
sudo systemctl reload nginx
```

#### 3.4.3 申请SSL证书

```bash
# 停止Nginx（80端口验证需要）
sudo systemctl stop nginx

# 申请证书
sudo certbot certonly --standalone -d your-domain.com -d www.your-domain.com

# 或使用nginx插件（推荐）
sudo systemctl start nginx
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 按提示操作：
# 1. 输入邮箱
# 2. 同意服务条款
# 3. 选择是否接收邮件
# 4. 选择是否重定向HTTP到HTTPS（推荐选2）
```

#### 3.4.4 启用HTTPS配置

编辑Nginx配置：

```bash
sudo nano /etc/nginx/sites-available/raspberrycloud
```

取消HTTPS server块的注释，并修改域名和证书路径：

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # ... 其他配置
}
```

重启Nginx：

```bash
sudo nginx -t
sudo systemctl restart nginx
```

#### 3.4.5 自动续期

```bash
# 测试续期
sudo certbot renew --dry-run

# 设置自动续期
sudo crontab -e

# 添加一行（每月1日凌晨3点检查续期）
0 3 1 * * certbot renew --quiet && systemctl reload nginx
```

### 3.5 验证外网访问

```
1. 使用手机4G/5G网络（非WiFi）
2. 浏览器访问: https://your-domain.com
3. 或访问: http://你的公网IP:8080（端口映射配置的端口）
4. 如能正常访问，配置成功
```

## 📱 方式四：移动端PWA配置

### 4.1 iOS（iPhone/iPad）

```
1. 使用Safari浏览器访问网站
2. 点击底部"分享"按钮（方框+箭头）
3. 滚动找到"添加到主屏幕"
4. 自定义名称（如"我的云盘"）
5. 点击"添加"
6. 主屏幕会出现应用图标
```

### 4.2 Android

```
1. 使用Chrome浏览器访问网站
2. 点击右上角菜单（三个点）
3. 选择"安装应用"或"添加到主屏幕"
4. 确认添加
5. 主屏幕会出现应用图标
```

### 4.3 PWA特性

- ✅ 离线访问（缓存基本界面）
- ✅ 推送通知（需额外配置）
- ✅ 后台同步
- ✅ 全屏体验

## 🔗 方式五：第三方客户端

### 5.1 ES文件浏览器（Android）

```
1. 安装ES文件浏览器
2. 侧边栏 → 网络 → 局域网
3. 点击"新建"
4. 填写信息：
   - 服务器: 树莓派IP
   - 用户名: Samba用户名
   - 密码: Samba密码
5. 连接
```

### 5.2 FE文件管理器（iOS）

```
1. 安装FE File Explorer
2. 添加连接 → Windows
3. 填写信息：
   - 显示名: 自定义
   - 主机: 树莓派IP
   - 用户名: Samba用户名
   - 密码: Samba密码
4. 连接
```

## 🔧 常见问题

### Q1: 无法访问局域网地址

```bash
# 检查树莓派IP
hostname -I

# 检查防火墙
sudo ufw status

# 检查服务状态
sudo systemctl status raspberrycloud
sudo systemctl status nginx

# Ping测试
ping 树莓派IP
```

### Q2: 外网无法访问

```
1. 确认端口映射配置正确
2. 确认DDNS解析正确:
   ping your-domain.com
3. 检查运营商是否封禁80/443端口
   - 如果封禁，使用8080/8443等端口
4. 检查防火墙规则
5. 查看路由器日志
```

### Q3: HTTPS证书申请失败

```bash
# 检查80端口是否被占用
sudo lsof -i :80

# 检查域名是否正确解析到公网IP
nslookup your-domain.com

# 查看Certbot日志
sudo tail -f /var/log/letsencrypt/letsencrypt.log

# 常见原因：
# - 域名未解析到公网IP
# - 80端口未正确映射
# - 防火墙阻止
```

### Q4: Samba挂载失败

```bash
# 检查Samba服务
sudo systemctl status smbd

# 测试配置
testparm

# 查看Samba日志
sudo tail -f /var/log/samba/log.smbd

# 重启Samba
sudo systemctl restart smbd
```

### Q5: 移动端网络流量消耗大

```
1. 启用图片压缩预览
2. 限制视频自动播放
3. 仅WiFi环境下自动同步
4. 使用文件列表而非缩略图视图
```

## 📊 性能优化建议

### 局域网优化

```
1. 使用有线网络（千兆网线）
2. 路由器QoS优先级设置
3. 关闭不必要的设备占用带宽
```

### 外网优化

```
1. 升级宽带上行速度（影响上传）
2. 使用CDN加速静态资源（高级）
3. 启用Nginx缓存
4. 文件分块传输
```

### Nginx缓存配置（可选）

```nginx
# 在http块中添加
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=10g inactive=60m use_temp_path=off;

# 在location块中添加
proxy_cache my_cache;
proxy_cache_valid 200 304 1h;
proxy_cache_valid any 1m;
```

## ✅ 配置验证清单

- [ ] 局域网Web访问正常
- [ ] 局域网磁盘挂载成功
- [ ] 端口映射配置完成
- [ ] DDNS解析正确
- [ ] HTTPS证书申请成功
- [ ] 外网访问测试通过
- [ ] 移动端PWA添加成功
- [ ] 防火墙规则正确
- [ ] 自动续期任务设置

## 🎉 完成

多端访问配置完成！现在你可以：
- 🏠 在家通过局域网快速访问
- 🌐 在外通过互联网安全访问
- 💻 电脑端像本地磁盘一样使用
- 📱 手机端随时随地管理文件

接下来查看 [安全加固指南](04-安全加固指南.md) 增强系统安全性。


